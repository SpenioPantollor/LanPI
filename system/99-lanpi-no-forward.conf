# LanPi Rule 3: never bridge/route between wlan0 (management) and eth0
# (TEST PORT). This overrides any accidental IP forwarding enabled by
# NetworkManager's AP 'shared' mode for the fallback access point.
net.ipv4.ip_forward=0
net.ipv6.conf.all.forwarding=0
